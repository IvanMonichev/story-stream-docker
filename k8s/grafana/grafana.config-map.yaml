apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-config
  namespace: story-stream
data:
  grafana.ini: |
    [server]
    root_url = http://localhost:3000
    serve_from_sub_path = true
    [security]
    admin_user = admin
    admin_password = admin
    admin_email = risen15@yandex.ru
