phantom:
  address: "api.music-demo.ru/load/cpu"
  uris:
    - "/"
  load_profile:
    load_type: rps
    schedule: const(5000, 5m)
  ssl: true
console:
  enabled: true
telegraf:
  enabled: false
influx:
  enabled: false
rcheck:
  enabled: false
