apiVersion: v1
kind: Service
metadata:
  name: pgadmin-service
  namespace: story-stream
spec:
  ports:
    - port: 3010
      targetPort: 80
      nodePort: 30080
  selector:
    app: pgadmin
  type: ClusterIP
